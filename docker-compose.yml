services:
  webapp:
    # image: webapp
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4000:4000"
    # volumes:
    #   - "/data"
  postgres:
    image: postgres
    restart: always
    environment:
      - POSTGRES_USER=dns147
      - POSTGRES_PASSWORD=123456
    volumes:
      - postgres:/var/lib/postgresql/data
    ports:
      - "5432:5432"
volumes:
  postgres: